{
  "name": "fv-validation",
  "version": "3.0.7",
  "description": "JavaScript Validation",
  "main": "build/index.js",
  "scripts": {
    "test": "npx mocha --compilers js:babel-core/register tests/",
    "tdd": "npx mocha --watch --compilers js:babel-core/register tests/",
    "build": "npx babel --preset env src -d build",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/finer-vision/Validation"
  },
  "author": "Finer Vision",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "mocha": "^5.0.1"
  }
}
