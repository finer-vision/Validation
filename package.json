{
  "name": "fv-validation",
  "version": "4.0.0",
  "description": "JavaScript Validation",
  "main": "build/index.js",
  "scripts": {
    "test": "npx mocha --require @babel/register tests",
    "tdd": "npx mocha --watch --require @babel/register tests",
    "build": "rm -rf build && npx babel src -d build",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/finer-vision/Validation"
  },
  "author": "Finer Vision",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.4",
    "@babel/plugin-proposal-class-properties": "^7.5.5",
    "@babel/preset-env": "^7.3.4",
    "@babel/register": "^7.0.0",
    "mocha": "^5.0.1"
  }
}
